#version 330 core

in vec4 colour;
in vec4 lighting;
in vec2 textureCoords;

uniform sampler2D textureSampler;

out vec4 finalColour;

void main()
{
	finalColour = texture(textureSampler, textureCoords);// * lighting;
	//finalColour = colour;// * lighting;
}